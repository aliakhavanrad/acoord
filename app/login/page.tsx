import Login from '@/app/(components)/Login';

export const metadata = {
  title: 'Login - ACOORD',
  description: 'Login to ACOORD and share your location-based audio memories.',
};

export default function LoginPage() {
  return <Login />;
}
